
/*list school in console.*/

var listHandler = {
    init: function () {
        var prv = $('#prvn'), citiesDrop = $('#citys'), countryDrop = $('#cityCntry');
        console.log('prv' + prv.length + 'city ' + citiesDrop.length + 'county' + countryDrop.length);

    }
}

listHandler.init();